export type Themes = {
   color: {
      primaryColor: string
      secondaryColor: string
      greyColor: string
   }
   font: {
      fontFamily: string
   }
   background: {
      primaryColor: string
   }
}
