export type Themes = {
   color: {
      primaryColor: string
      secondaryColor: string
      greyColor: string
      disableColor:string
   }
   font: {
      fontFamily: string
   }
   background: {
      primaryColor: string
   }
}
